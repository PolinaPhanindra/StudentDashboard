@model WebApplication1.Models.Appraisal

@{
    ViewBag.Title = "Delete";
    
}

<h2>Delete</h2>
@if (Model == null)
{
    @Html.Raw(ViewBag.AlertMsg)
}
else { 
<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Appraisal</h4>
    <hr />

    <dl class="dl-horizontal">
        <dt>`
            @Html.DisplayNameFor(model => model.EmployeeId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EmployeeId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Appraiser)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Appraiser)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Appraisee)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Appraisee)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Rating)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Rating)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Applicable)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Applicable)
        </dd>

    </dl>
    
    @using (Html.BeginForm("Delete", "Appraisal", new { @id = Model.EmployeeId }))
    {
        @*@Html.AntiForgeryToken()*@

        <div>
            @Html.DisplayFor(model => model.EmployeeId, new { @id = Model.EmployeeId })
        </div>

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
}
